# 数据爬取

## 使用库
~~~
playwright
playwright-stealth
~~~

## 步骤
~~~bash

git clone https://github.com/xllyll/crawler_data.git
cd crawler_data
# 构建venv 虚拟环境
python -m venv .venv
or
python3 -m venv .venv

#下载依赖
pip install playwright playwright-stealth
#安装依赖
playwright install
#运行代码
run

~~~